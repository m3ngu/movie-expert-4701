#summary One-sentence summary of this page.

= Rules =

  # Create (user-likes-action =w) objects
  # Create (user-likes-comedy =w) objects
  # ...
  # Create (user-likes-director =dname =w) objects
  # Create (user-likes-era =ename =w) objects
  # Create (user-likes-actor =aname =w) objects
  # Filter (user-likes-actor =aname =w) objects for =w > 3
  # Create (recommendation =mname =w) objects
  # Remove (recommendation =mname =w) objects if =mname is in user input  
  # Sort (recommendation =mname =w) objects by descending =w
  # Return first 10